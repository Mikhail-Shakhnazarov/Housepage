[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "housepage-projection"
version = "0.1.0"
description = "Python semantic reference implementation for Housepage Scan-Deal-Act"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
